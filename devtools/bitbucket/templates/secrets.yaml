{{- if .Values.bitbucketSecrets.license }}
apiVersion: v1
kind: Secret
metadata:
  name: bitbucket-license
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  license-key: {{ .Values.bitbucketSecrets.license | quote }}
---
{{- else if .Values.bitbucketSecrets.licenseSsmParameter }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bitbucket-license
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: bitbucket-license
    creationPolicy: Owner
  data:
    - secretKey: license-key
      remoteRef:
        key: {{ .Values.bitbucketSecrets.licenseSsmParameter | quote }}
---
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: bitbucket-sysadmin
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  username: {{ .Values.bitbucketSecrets.admin.username | quote }}
  displayName: {{ .Values.bitbucketSecrets.admin.displayName | quote }}
  emailAddress: {{ .Values.bitbucketSecrets.admin.emailAddress | quote }}
---
{{- if .Values.bitbucketSecrets.adminPasswordSsmParameter }}
# Patches the "password" key into the bitbucket-sysadmin Secret above.
# creationPolicy: Merge only adds/updates this one key — it never owns or
# deletes the Secret, so it can't race the plain Secret manifest above it.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bitbucket-sysadmin-password
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: bitbucket-sysadmin
    creationPolicy: Merge
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.bitbucketSecrets.adminPasswordSsmParameter | quote }}
---
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: bitbucket-db
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  username: {{ .Values.bitbucketSecrets.db.username | quote }}
  password: {{ .Values.bitbucketSecrets.db.password | quote }}
