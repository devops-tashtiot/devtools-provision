apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bitbucket-shared-home
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 10Gi
